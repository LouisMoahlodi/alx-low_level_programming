#include "main.h"

/**
 * alloc_grid - Function returns a pointer to a 2D array of integers
 *
 * @width: Integer to display the rows
 * @height: Integer to display columns
 *
 * Return: Double pointer:
 * or "NULL" if failure: or if width or height is 0 || (-)
 */
int **alloc_grid(int width, int height)
{
	int **new;

	int row, col, num;

	if (width <= 0 || height <= 0)
	{
		return (NULL);
	}
	/*Intializing the 2D array */
       int array2D[	
}
