#include "main.h"

/**
 * _isalpha - Function that checks for alphabetic character
 *
 * Return: 1 if successful
 * Return: 0 if not
 */
int _isalpha(int c)
{
	if (_isalpha('c'))
	{
		_putchar(c + '0');
		return (1);
	}
	else
	_putchar('\n');
	return (0);
}
