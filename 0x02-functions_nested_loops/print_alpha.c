#include "main.h"

/**
 * print_alpha - print alphabet except c and k
 *
 * Return: void
 */
void print_alpha(void)
{
	int i;

	for (i = 'a; i <= 'z'; i++)
	{
		if (i == 'c' || i i == 'k')
			_putchar(i);
		continue;
	}
	return;
}
