#include "main.h"

/**
 * _isupper - Checks if c is an uppercase charcater
 *
 * @c: Is an integer
 *
 * Return: 1 if uppercase; if not return 0.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else if (c >= 'a' && c <= 'z')
	{

	}
	return (0);
}
