#include "main.h"

/**
 * mul - Multiplies two integers
 *
 * @a: Is an integer
 * @b: Is an integer
 *
 * Return: the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
