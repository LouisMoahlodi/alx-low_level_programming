#include "main.h"

/**
 * _abs - Function to computes the absolute value of an interger
 *
 * @x: is an integer value
 *
 * Return: Always 0 (Success)
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (x * -1);
	}
	else if (x == 0)
	{
		return (x);
	}
	else
	{
		return (x);
	}

}
